import React from 'react';

export const metadata = {
    title: "SocialNst | Profile",
};

const page = () => {
    return (
        <div>
            Setting
        </div>
    );
};

export default page;