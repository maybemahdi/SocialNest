import React from 'react';

export const metadata = {
    title: "SocialNst | Profile",
};

const page = () => {
    return (
        <div>
            Profile
        </div>
    );
};

export default page;