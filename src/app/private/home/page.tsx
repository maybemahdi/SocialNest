import React from 'react';

export const metadata = {
    title: "SocialNst | Home",
};

const Page: React.FC = () => {
    return (
        <div>
            Home
        </div>
    );
};

export default Page;