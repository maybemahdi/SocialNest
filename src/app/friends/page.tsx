import React from 'react';

export const metadata = {
    title: "SocialNst | Friends",
};

const page = () => {
    return (
            <div>
                Friends
            </div>
    );
};

export default page;