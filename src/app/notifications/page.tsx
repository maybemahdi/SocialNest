import React from 'react';

export const metadata = {
    title: "SocialNst | Notifications",
};

const page = () => {
    return (
        <div>
            Notifications
        </div>
    );
};

export default page;